2.
CREATE TABLE CUSTOMER (
    user_id int,
    name varchar(255)
);
You have made changes to the database.

CREATE TABLE food_order(order_id int, user_id int, food_item varchar(255), order_date date);
You have made changes to the database.

1.
SELECT C.name, F.food_item
FROM CUSTOMER C, food_order F
WHERE C.user_id = F.user_id;

output:
name	food_item
harsh	pizza
hk		burger
hk		pizza

2.

SELECT C.name, F.food_item, F.order_date
FROM CUSTOMER C, food_order F
WHERE C.user_id = F.user_id
AND F.order_date BETWEEN '2019-07-29' AND '2019-08-29';

output:

name		food_item		order_date
hk			pizza			2019-08-01
hk			burger			2019-08-01
harsh		pizza			2019-08-01
